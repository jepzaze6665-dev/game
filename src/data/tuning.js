// Per-unit designer override on top of the tier curve (1 = tier curve only).
// Use sparingly for units whose real-match performance the value formula misjudges.
export const TUNE = { m_plague: 0.8, r_flame: 1.15, r_tesla: 1.1, r_spider: 1.1, r_combat: 1.1 };
